<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.junemat.matserver.views.repository.testMapper">
	<select id="testSql" parameterType="String" resultType="Integer">
		SELECT COUNT(*)   
		  FROM PORTAL_APP_INFO
	</select>	
</mapper>